server:
  tomcat:
    max-http-form-post-size: 256MB
    max-swallow-size: 256MB

spring:
  datasource:
    url: jdbc:mysql://ion-database-rds.czmtnxk070k4.ap-northeast-2.rds.amazonaws.com:3306/ion?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: admin
    password: IOn2025!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 600000
      connection-timeout: 30000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        jdbc.time_zone: Asia/Seoul

  # (MVC) 업로드 용량 제한
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 220MB
      file-size-threshold: 2MB

  # (WebFlux) 디코딩/멀티파트 버퍼
  codec:
    max-in-memory-size: 256MB
  webflux:
    multipart:
      max-in-memory-size: 256MB

logging:
  level:
    root: INFO
    org.springframework.boot.autoconfigure: INFO
    org.hibernate.SQL: INFO

uploads:
  voice-dir: /var/app/uploads/voice

# ffmpeg 실행 경로 및 변환 타임아웃(서비스 코드 @Value 사용)
media:
  ffmpeg:
    path: ffmpeg
  convert:
    timeoutSec: 120

analysis:
  python:
    baseUrl: http://10.0.1.91:9095
    connectTimeoutMs: 5000
    readTimeoutMs: 500000

workbook:
  python:
    create-base-url: http://10.0.1.91:9096
    feedback-base-url: http://10.0.1.91:9098
    read-timeout-ms: 120000

chatbot:
  python:
    base-url: http://10.0.1.91:9097
    connect-timeout-ms: 5000
    read-timeout-ms: 600000

demo:
  enabled: true
  user-id: u001

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
      probes:
        enabled: true
